Source : https://geneweb.tuxfamily.org/wiki/expert
Récupération : web_fetch (le tableau des variables bvar semble avoir été tronqué à l'extraction — complété ci-dessous par un extrait de recherche séparé qui liste ces variables)

---

The expert corner

Template programming (%if; ... %end;)

The template mechanism provides a very powerful mechanism to construct HTML (or other) pages according to the content of a GeneWeb base. This section is not intending to give an exhaustive description of the corresponding language. Through some examples extracted from gw/etc/perso.txt, it provides a glimpse of the possibility. The last two examples are extracted from a template that generates LaTeX rather than HTML.

For further analysis, explore the various other template files, and on last resort, explore the OCaml source files!!

Programming constructs:
%define; ... %end;
%if; [%elseif;] [%else;] %end;
%foreach; ... %end;
%apply; ... [%with;... [%and;] ...%end;]
%nl; %nn; %sp;

[Several template code examples follow, including a married_to macro definition,
a conditional for adding/modifying a portrait picture, and a LaTeX-generating
index macro (hgindex) — see gwformat.txt and templates section of the manual
for related, cleaner examples already documented.]

This list does not claim to be exhaustive (but pretty close!!).

Note for has_event (v7)
Note that the has_event function does not consider birth, baptism, death and burial as "events" per se. If you want those events to be taken into account in this function, then you have to add display_timeline=yes in your .gwf parameter file.
Note also that if display_timeline=no, then has_event always returns False.

Macros (%x)
See Macros for a description of their use.
Below is the result of calling all 26 lower case possible macros: This table is automatically generated by inserting a bit of HTML/GeneWeb code into the notes of one person in your base.

Commands (m=XXX)
List of the commands appearing in URL. This list does not claim to be exhaustive, and does not "explain" what each command does!
"DAG" * / "DOC" (obsolete) / "IM" * / "IMH" * / "MOD_WDOC" (obsolete) / "MOD_WDOC_OK" (obsolete) / "PS" / "RLM" * / "SRC" * / "WDOC" (obsolete)
(Commands marked with * are documented elsewhere on the wiki.)

Variables (evar and bvar)
GeneWeb proposes a general purpose mechanism allowing the user/programmer to extend at will the notion of variable.

If a new variable is defined in the .gwf configuration file, it becomes possible to test for its value in a template file:
%if; (bvar.new_var = "sometext")
    ... perform some specific action ...

Similarly, a new URL variable (e.g. localhost:2317/base?p=first-name;n=last-name;lang=fr;tree=3) can be tested with:
%if; (evar.tree = 3)
    ... display 3 generation tree ...

The value of such variables can be inserted in a template with %evar.tree;, e.g.:
<title>Tree with %evar.tree; generations</title>

The special "var" mechanism: defining var_who=Bond, James Bond in .gwf lets the macro %vwho; in a note or template produce "Bond, James Bond". Used to facilitate signature of sources, e.g.:
var_henri=<a href="http://site/base?p=Firstname;n=lastname">First_name Lastname</a>
(no %x or %macro; allowed in the value of the parameter)

Building trees through direct url construction: see trees or trees/fr.

---
COMPLÉMENT (extrait de recherche distinct, liste des variables bvar — probablement la table
tronquée dans le fetch principal ci-dessus) :

always_surname, yes
auth_file
base_notes_title
body_prop
can_kill_ancestors
can_send_image, no
conf.base_env
counter, no
death_symbol, †
default_sosa_ref
disable_forum, yes
dump_bad_images, yes
expand_env
fast_alphabetic, yes
forum_exclude_file
friend_passwd
friend_passwd_file
has_events
history, yes
history_diff
history_path
image_prefix
images_path
latest_event
long_date, yes
max_anc_tree
max_ancestor_implex
max_cousins
max_desc_level
max_desc_tree
max_images_size
max_nb_update
max_rlm_nbr
moderator_file
notes_alias_file
notify_change
passwd_
place
place_surname_link_to_ind, yes
places_inverted, yes
print_advanced_title, yes
propose_add_family, no
ptempl, yes
red_if_not_exist
short_place_threshold
show_query_time
static_path
template
var_
visitor_access, no
wizard_descr_file
wizard_passwd_file

(Autre liste, probablement une section différente de la même page ou d'une page liée, avec des noms non triés alphabétiquement de façon identique) :
access_by_key always_surname auth_file authorized_wizards_notes base_notes_title birth_symbol can_kill_ancestors can_send_image carrousel com counter css css_prop datalist_fn datalist_occu datalist_place datalist_sn datalist_src datalist_...(liste tronquée dans l'extrait obtenu)

Note : "The bvar name is sometimes followed by one of its options (yes/no)" — précision donnée par la page elle-même.

---
COMPLÉMENT 2 (extrait de recherche distinct, liste alphabétique BEAUCOUP plus longue des
commandes m=XXX que celle du fetch principal — probablement une section différente ou plus
complète de la même page "Commands (m=XXX)") :

"A" "AD" "ADD_FAM" "ADD_FAM_OK" "ADD_IND" "ADD_IND_OK" "ADD_PAR" "ADD_PAR_OK" "AM" "AN" "ANM"
"AS_OK" "C" "CAL" "CAST" "CHANGE_WIZ_VIS" "CHG_CHN" "CHG_CHN_OK" "CHG_EVT_FAM_ORD"
"CHG_EVT_FAM_ORD_OK" "CHG_EVT_IND_ORD" "CHG_EVT_IND_ORD_OK" "CHG_FAM_ORD" "CHG_FAM_ORD_OK"
"CONN_WIZ" "D" "DAG" * "DATE" "DEL_FAM" "DEL_FAM_OK" "DEL_IMAGE" "DEL_IMAGE_OK" "DEL_IND"
"DEL_IND_OK" "F" "FIXBASE" "FIXBASE_OK" "FORUM_ADD" "FORUM_ADD_OK" [liste tronquée dans
l'extrait obtenu — se poursuit alphabétiquement au-delà de FORUM_ADD_OK, non capturé].

(Cette liste confirme au passage les commandes CONN_WIZ et VIEW_WIZNOTES/MOD_WIZNOTES/WIZNOTES
déjà documentées séparément via wiznotes.txt, ainsi que DAG déjà documenté via gwd.txt.)

