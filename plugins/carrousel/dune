(alias 
    (name plugin)
    (deps (source_tree assets) (file META) plugin_carrousel.cmxs)
)

(executable
  (name plugin_carrousel)
  (libraries geneweb geneweb.gwd_lib geneweb.wserver plugin_v7_lib)
  (flags -linkall)
  (modes (native plugin))
)
