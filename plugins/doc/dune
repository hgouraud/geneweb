(alias 
    (name plugin) 
   (deps (source_tree assets) (file META) plugin_doc.cmxs)
)

(executable
  (name plugin_doc)
  (libraries geneweb geneweb.gwd_lib geneweb.wserver)
  (flags -linkall)
  (modes (native plugin))
)
